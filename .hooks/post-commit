#!/bin/bash

# Set irssi password
pushd .irssi > /dev/null

if [ ! -f 'passwd' ]; then
  echo 'No passwd file found.' >&2
else
  sed -e "s/%pass%/$(cat passwd)/" config.skel > config
fi

popd > /dev/null

